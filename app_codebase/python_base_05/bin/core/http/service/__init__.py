"""HTTP “service” side: the route table and how incoming requests reach the code you registered.

See :mod:`core.http.service.routes` for ``application_routes``, dispatch, and the Flask app factory.
"""
