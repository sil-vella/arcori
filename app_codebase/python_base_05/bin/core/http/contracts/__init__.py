"""Stable interfaces the rest of the app can code against: how to register URLs and how to build
JSON responses, without tying feature modules to Flask details.

Concrete implementations live under ``core.http.response`` and ``core.http.service``.
"""
